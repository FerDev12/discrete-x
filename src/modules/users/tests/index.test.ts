describe('User module', () => {
  it('Successfully creates a user', async () => {});

  it('Successfully updates a user email address', () => {});

  it('Successfully deletes a user', () => {});

  it('Fails to create a user when its given an invalid user Id', () => {});

  it('Fails to delete a user when its given an invalid user Id', () => {});
});
